let itemsList = ["Ghee", "Oil", "Slats", "Towl", "Door mate", "Spark Due"];

// Access items from itemsList using for loop
for (item in itemsList) {
    console.log(itemsList[item]);
}

// Another way to access array items
let lengthOfItems = itemsList.length;

// for (i = 0; i < lengthOfItems; i++) {
//     console.log(itemsList[i])
// }